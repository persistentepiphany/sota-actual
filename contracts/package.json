{
  "name": "sota-flare-contracts",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:flare": "hardhat run scripts/deploy-flare.ts --network flareCoston2",
    "deploy:local": "hardhat run scripts/deploy-flare.ts --network hardhat",
    "deploy:legacy": "hardhat run scripts/deploy.ts --network neoxTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.3.10",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.10.6",
    "dotenv": "^16.4.5",
    "hardhat": "^2.20.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@flarenetwork/flare-periphery-contract-artifacts": "^0.1.40",
    "@openzeppelin/contracts": "^5.0.2"
  }
}
